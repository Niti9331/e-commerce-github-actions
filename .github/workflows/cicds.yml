# Goal is to create cicd repo (dummy)
name: CICD Demo

on:
  push: 

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: Clone the code
        run: echo "Code cloned"
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: Build the code
        run: echo "Code built"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test the code
        run: echo "Code tested and passed"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the code
        run: echo "Code deployed"
